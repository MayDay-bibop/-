#include <stdio.h>

#define MAXLINE 1000

int main(int argc, char *argv[])
{
    if (argc != 2) {
        printf("Usage: %s <filename.c>\n", argv[0]);
        return 1;
    }

    FILE *myfile = fopen(argv[1], "rt");
    if (!myfile) {
        perror("fopen");
        return 1;
    }
}